/**
 * Math object
 * - Math.Pi
 * - Math.round()
 * - Math.abs()
 * - Math.ceil()
 * - Math.floor()
 * - Math.random()
 * - Math.min()
 * - Math.max()
 */
/**
 * Callback
 * Là hàm được truyền qua đối số khi gọi một hàm khác
 * 
 * 1. Là hàm
 * 2. Được truyền qua đối số
 * 3. Được gọi lại (trong hàm nhận đối số)
 */
